# Jobly — React frontend + Express backend

Setup: `cd jobly-backend && npm install && npm run dev` then `cd jobly-frontend && npm install && npm run dev`

Env (backend `.env`): `DATABASE_URL=postgresql://localhost:5432/jobly` and `SECRET_KEY=secret-dev`

Env (frontend `.env`): `VITE_BASE_URL=http://localhost:3001`
